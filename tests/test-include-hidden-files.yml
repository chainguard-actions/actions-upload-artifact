name: Test include hidden files in artifact
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create test directory with hidden and visible files
        run: |
          mkdir -p /tmp/hidden-files-test
          echo "visible content" > /tmp/hidden-files-test/visible.txt
          echo "hidden content" > /tmp/hidden-files-test/.hidden-file
          echo "another hidden" > /tmp/hidden-files-test/.env

      - id: upload
        uses: .
        with:
          name: hidden-files-artifact
          path: /tmp/hidden-files-test/
          include-hidden-files: 'true'

      - name: Assert artifact-id is non-empty (hidden files included)
        env:
          ARTIFACT_ID: ${{ steps.upload.outputs.artifact-id }}
        run: |
          echo "artifact-id: $ARTIFACT_ID"
          if [ -z "$ARTIFACT_ID" ]; then
            echo "FAIL: artifact-id is empty, upload failed"
            exit 1
          fi
          echo "PASS: artifact-id=$ARTIFACT_ID"

      - name: Assert artifact-url is non-empty
        env:
          ARTIFACT_URL: ${{ steps.upload.outputs.artifact-url }}
        run: |
          echo "artifact-url: $ARTIFACT_URL"
          if [ -z "$ARTIFACT_URL" ]; then
            echo "FAIL: artifact-url is empty"
            exit 1
          fi
          echo "PASS: artifact-url=$ARTIFACT_URL"
