name: Test if-no-files-found warn behavior
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: upload
        uses: .
        with:
          name: no-files-artifact
          path: /tmp/nonexistent-path-that-does-not-exist/**
          if-no-files-found: warn

      - name: Assert action succeeded (warn does not fail)
        run: |
          echo "outcome: ${{ steps.upload.outcome }}"
          OUTCOME="${{ steps.upload.outcome }}"
          if [ "$OUTCOME" != "success" ]; then
            echo "FAIL: expected success with if-no-files-found=warn, got outcome=$OUTCOME"
            exit 1
          fi
          echo "PASS: action succeeded with warn when no files found"
