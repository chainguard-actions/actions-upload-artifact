name: Test basic upload with path input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create test file to upload
        run: |
          mkdir -p /tmp/test-artifact
          echo "hello world" > /tmp/test-artifact/hello.txt
      - id: upload
        uses: .
        with:
          name: test-artifact
          path: /tmp/test-artifact/hello.txt
          if-no-files-found: warn
        continue-on-error: true
      - name: Assert action ran (outputs present or action attempted)
        run: |
          echo "artifact-id: ${{ steps.upload.outputs.artifact-id }}"
          echo "artifact-url: ${{ steps.upload.outputs.artifact-url }}"
          echo "artifact-digest: ${{ steps.upload.outputs.artifact-digest }}"
          echo "outcome: ${{ steps.upload.outcome }}"
          # In act environment, upload may fail due to no GitHub API access.
          # We verify the action was invoked and produced some outcome.
          if [ "${{ steps.upload.outcome }}" = "" ]; then
            echo "FAIL: action did not run at all"
            exit 1
          fi
          echo "PASS: action ran with outcome=${{ steps.upload.outcome }}"
